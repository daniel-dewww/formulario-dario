import { FindValueArrayPipe } from './find-value-array.pipe';

describe('FindValueArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new FindValueArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
