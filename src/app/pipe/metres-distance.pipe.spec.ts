import { MetresDistancePipe } from './metres-distance.pipe';

describe('MetresDistancePipe', () => {
  it('create an instance', () => {
    const pipe = new MetresDistancePipe();
    expect(pipe).toBeTruthy();
  });
});
