import { ShortcutDescriptionPipe } from './shortcut-description.pipe';

describe('ShortcutDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortcutDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
