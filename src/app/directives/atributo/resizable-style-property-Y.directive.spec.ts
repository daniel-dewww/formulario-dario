import { ResizableStylePropertyYDirective } from './resizable-style-property-Y.directive';

describe('ResizableStylePropertyYDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new ResizableStylePropertyYDirective();
  //   expect(directive).toBeTruthy();
  // });
});
