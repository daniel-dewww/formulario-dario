import { MoveScrollDownDirective } from './move-scroll-down.directive';

describe('MovScrollDownDirective', () => {
  it('should create an instance', () => {
    const directive = new MoveScrollDownDirective();
    expect(directive).toBeTruthy();
  });
});
