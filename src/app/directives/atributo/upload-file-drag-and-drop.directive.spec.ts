import { UploadFileDragAndDropDirective } from './upload-file-drag-and-drop.directive';

describe('UploadFileDragAndDropDirective', () => {
  it('should create an instance', () => {
    const directive = new UploadFileDragAndDropDirective();
    expect(directive).toBeTruthy();
  });
});
