import { IdGeneric } from './typesKeyword';

export class CurrencyType {
    code?: string;
    id?: IdGeneric;
    name?: string;
    symbol?: string
    enable?: boolean
}


export class CreditTypes {
    code ?: string;
    enable ?: boolean;
    name ?: string;
}